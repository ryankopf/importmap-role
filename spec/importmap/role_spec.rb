# frozen_string_literal: true

RSpec.describe Importmap::Role do
  it "has a version number" do
    expect(Importmap::Role::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
